import Link from "next/link";
import {PageContainer,PageHeader,Surface} from "@/components/ui/design-system";
import {DeleteAccountForm} from "@/components/account/DeleteAccountForm";
export default function DeletePage(){return <PageContainer narrow><PageHeader title="Delete Account" description="Permanently close your AXVital account. This cannot be undone through the app."/><Surface className="mt-6 space-y-4"><h2 className="text-lg font-semibold">Before you continue</h2><ul className="list-disc space-y-2 pl-5"><li>Your profile and user-owned health records, plans, experiments, and recaps will be removed from the active application database.</li><li>Your Stripe customer will be closed and any active subscriptions canceled immediately. Remaining paid access ends; this action does not request or promise a refund.</li><li>Your sign-in account will be deleted. You cannot recover it by signing in again.</li><li>Previously downloaded exports are not deleted. Provider billing records, backups, and security records may remain according to the providers’ and operator’s retention obligations; this is not an instant erasure guarantee for every system.</li></ul><p><Link className="font-semibold text-blue-700 underline" href="/settings/data">Export your data first</Link> if you want to keep a copy. For subscription cancellation without deleting health records, use <Link className="text-blue-700 underline" href="/settings/billing">Billing</Link>.</p><DeleteAccountForm enabled={process.env.AXVITAL_ACCOUNT_DELETION_ENABLED==="true"}/></Surface></PageContainer>}
