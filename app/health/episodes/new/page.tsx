import { EpisodeForm } from "@/components/episodes/EpisodeForm";
import { ButtonLink, PageContainer, PageHeader, Surface } from "@/components/ui/design-system";
export default async function Page({searchParams}:{searchParams:Promise<{condition?:string}>}){const{condition}=await searchParams;return <PageContainer narrow><ButtonLink href={condition?`/health/conditions/${condition}`:"/health"} variant="tertiary" className="mb-4 -ml-4">← Back</ButtonLink><PageHeader eyebrow="Self-reported health" title="Log an Episode" description="Track a flare, outbreak, attack, or another meaningful episode related to a condition you manage."/><Surface className="mt-6"><EpisodeForm conditionId={condition}/></Surface><p className="mt-5 text-sm leading-6 text-slate-500">Symptoms remain separate health records. Logging symptoms does not confirm or diagnose an episode.</p></PageContainer>}
