import assert from"node:assert/strict";import test from"node:test";import{comparePeriods,validateDraft}from"./experiments.ts";const valid={name:"Hydration and energy",hypothesis:"I believe hydration may affect my daily energy.",interventionType:"hydration_target",interventionName:"Drink 80 oz daily",outcomeType:"energy",outcomeName:"Energy",studyDesign:"baseline_intervention"as const,baselineDays:7,interventionDays:21,adherenceTarget:80};test("complete draft validates",()=>assert.equal(validateDraft(valid),null));test("prescription dose changes are blocked",()=>assert.match(validateDraft({...valid,interventionName:"Decrease prescription medication dose"})??"",/cannot direct/));test("unsafe dehydration is blocked",()=>assert.match(validateDraft({...valid,hypothesis:"I will test intentional dehydration for energy"})??"",/unsafe/));test("period comparison excludes nulls and avoids causal language",()=>{const r=comparePeriods([5,null,7],[7,8,null]);assert.equal(r.baselineCount,2);assert.equal(r.interventionCount,2);assert.equal(r.baselineValue,6);assert.equal(r.interventionValue,7.5);assert.equal(r.change,1.5);assert.doesNotMatch(r.text,/caused|proven|effective/i)});test("zero baseline avoids invalid percent",()=>assert.equal(comparePeriods([0],[1]).percent,null));
