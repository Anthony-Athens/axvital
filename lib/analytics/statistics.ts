export const clean=(values:Array<number|null|undefined>)=>values.filter((value):value is number=>typeof value==="number"&&Number.isFinite(value));export function mean(values:Array<number|null|undefined>){const x=clean(values);return x.length?x.reduce((a,b)=>a+b,0)/x.length:null}export function median(values:Array<number|null|undefined>){const x=clean(values).sort((a,b)=>a-b);if(!x.length)return null;const m=Math.floor(x.length/2);return x.length%2?x[m]:(x[m-1]+x[m])/2}export function percentChange(current:number,baseline:number){return Math.abs(baseline)<0.0001?null:(current-baseline)/Math.abs(baseline)*100}export const round=(value:number,digits=1)=>Number(value.toFixed(digits));
