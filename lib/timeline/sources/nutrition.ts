import type{TimelineSource}from"../types.ts";
export const getNutritionEvents:TimelineSource=async({client,userId,start,end})=>{const{data,error}=await client.from("nutrition_entries").select("id,entry_type,meal_type,title,consumed_at,notes,items:nutrition_entry_items(source_name,calories,protein_grams)").eq("user_id",userId).is("deleted_at",null).gte("consumed_at",start).lt("consumed_at",end);if(error)throw error;return(data??[]).map(row=>{const items=(row.items??[])as Array<{source_name:string;calories:number|null;protein_grams:number|null}>;const calories=items.reduce((n,x)=>n+Number(x.calories??0),0),protein=items.reduce((n,x)=>n+Number(x.protein_grams??0),0),meal=row.entry_type==="meal"||items.length>1;return{id:`nutrition_entry:${row.id}`,sourceId:row.id,sourceType:"nutrition_entry",eventType:meal?"meal" as const:"food" as const,occurredAt:row.consumed_at,endedAt:null,title:row.title??items[0]?.source_name??"Food",subtitle:`${items.length>1?`${items.length} items · `:""}${Math.round(calories)} kcal · ${Math.round(protein*10)/10} g protein`,description:row.notes,status:null,metadata:{itemCount:items.length,items:items.map(x=>x.source_name),calories,proteinGrams:protein,mealType:row.meal_type},editable:true,deletable:true,detailHref:"/health/nutrition",editHref:"/health/nutrition"};});};
