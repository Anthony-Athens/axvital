import type{TimelineSource}from"../types.ts";
export const getCheckinEvents:TimelineSource=async({client,userId,startDate,endDate})=>{const{data,error}=await client.from("daily_checkins").select("id,checkin_date,energy_score,mood_score,sleep_quality,exercise_level,nutrition_quality,stress_level,alcohol,weight,notes,created_at,updated_at").eq("user_id",userId).gte("checkin_date",startDate).lte("checkin_date",endDate);if(error)throw error;return(data??[]).map(row=>({id:`daily_checkin:${row.id}`,sourceId:row.id,sourceType:"daily_checkin",eventType:"checkin" as const,occurredAt:row.updated_at??row.created_at,endedAt:null,title:"Daily Check-In",subtitle:`Sleep ${row.sleep_quality} · Energy ${row.energy_score} · Mood ${row.mood_score}`,description:row.notes,status:"completed",metadata:{energy:row.energy_score,mood:row.mood_score,sleepQuality:row.sleep_quality,exerciseLevel:row.exercise_level,nutritionQuality:row.nutrition_quality,stressLevel:row.stress_level,alcohol:row.alcohol,weight:row.weight},editable:true,deletable:false,detailHref:"/today#daily-checkin",editHref:"/today#daily-checkin"}));};
