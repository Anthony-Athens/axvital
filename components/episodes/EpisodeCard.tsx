import Link from "next/link";
import { durationHours, episodeDay, peakSeverity } from "@/lib/episodes/episodes";
import type { ConditionEpisode } from "@/lib/episodes/types";
export function EpisodeCard({episode}:{episode:ConditionEpisode}){const label=episode.user_condition?.condition?.preferred_episode_label||"Episode",name=episode.user_condition?.condition?.name??episode.user_condition?.custom_condition_name??"Condition",hours=durationHours(episode),peak=peakSeverity(episode);return <article className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-blue-700">{name}</p><h3 className="font-semibold text-slate-900">{episode.title||`${name} ${label}`}</h3></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize">{episode.status}</span></div><p className="mt-2 text-sm text-slate-600">{new Date(episode.started_at).toLocaleString()} · {episode.status==="ongoing"?`Day ${episodeDay(episode.started_at)}`:hours<24?`${Math.round(hours)} hr`:`${(hours/24).toFixed(1)} days`}</p><p className="mt-1 text-sm text-slate-600">{peak?`Peak severity ${peak} / 10 · `:""}{episode.symptom_links?.length??0} symptoms linked{episode.functional_impact?` · ${episode.functional_impact} impact`:""}</p><Link href={`/health/episodes/${episode.id}`} className="mt-3 inline-flex min-h-11 items-center font-semibold text-blue-700" aria-label={`View ${name} ${label} started ${new Date(episode.started_at).toLocaleDateString()}`}>View {label}</Link></article>}
