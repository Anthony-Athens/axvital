"use client";
import { FormEvent, useEffect, useMemo, useRef, useState } from "react";
import { Button, InlineNotice, controlClass } from "@/components/ui/design-system";
import { addUserCondition, filterCatalog } from "@/lib/conditions/conditions";
import type { CatalogCondition, ConditionCategory, UserCondition } from "@/lib/conditions/types";
import { createClient } from "@/lib/supabase/browser";
import { ConditionDetailsFields, type ConditionFormValues } from "./ConditionDetailsFields";

const initialDetails: ConditionFormValues = { status: "active", diagnosedOn: "", diagnosedYear: "", isPrimary: false, notes: "" };
export function AddConditionDialog({ open, categories, catalog, onClose, onAdded }: { open: boolean; categories: ConditionCategory[]; catalog: CatalogCondition[]; onClose: () => void; onAdded: (item: UserCondition) => void }) {
  const dialogRef = useRef<HTMLDivElement>(null); const searchRef = useRef<HTMLInputElement>(null); const [search, setSearch] = useState(""); const [category, setCategory] = useState(""); const [selected, setSelected] = useState<CatalogCondition | null>(null); const [customMode, setCustomMode] = useState(false); const [customName, setCustomName] = useState(""); const [details, setDetails] = useState(initialDetails); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  const results = useMemo(() => filterCatalog(catalog, category, search), [catalog, category, search]);
  useEffect(() => { if (!open) return; const previous = document.activeElement as HTMLElement | null; const oldOverflow = document.body.style.overflow; document.body.style.overflow = "hidden"; requestAnimationFrame(() => searchRef.current?.focus()); function keydown(event: KeyboardEvent) { if (event.key === "Escape") { event.preventDefault(); onClose(); } if (event.key !== "Tab") return; const focusable = Array.from(dialogRef.current?.querySelectorAll<HTMLElement>('button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled])') ?? []); if (!focusable.length) return; const first = focusable[0], last = focusable.at(-1)!; if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); } } document.addEventListener("keydown", keydown); return () => { document.removeEventListener("keydown", keydown); document.body.style.overflow = oldOverflow; previous?.focus(); }; }, [open, onClose]);
  if (!open) return null;
  async function submit(event: FormEvent) { event.preventDefault(); if (saving) return; setSaving(true); setError(""); try { const item = await addUserCondition(createClient(), { conditionId: selected?.id, customName: customMode ? customName : null, status: details.status, diagnosedOn: details.diagnosedOn || null, diagnosedYear: details.diagnosedYear ? Number(details.diagnosedYear) : null, isPrimary: details.isPrimary, notes: details.notes }); onAdded(item); onClose(); } catch (caught) { setError(caught instanceof Error ? caught.message : "We couldn’t add this condition."); } finally { setSaving(false); } }
  return <div className="fixed inset-0 z-[70] bg-slate-950/45" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div ref={dialogRef} role="dialog" aria-modal="true" aria-labelledby="add-condition-title" className="absolute inset-x-0 bottom-0 max-h-[94dvh] overflow-y-auto rounded-t-2xl bg-white p-5 sm:inset-x-auto sm:bottom-auto sm:left-1/2 sm:top-1/2 sm:w-[min(94vw,44rem)] sm:-translate-x-1/2 sm:-translate-y-1/2 sm:rounded-2xl sm:p-6"><div className="flex items-start justify-between gap-4"><div><h2 id="add-condition-title" className="text-xl font-semibold text-slate-900">Add a condition</h2><p className="mt-1 text-sm text-slate-600">Choose a catalog condition or add your own.</p></div><button type="button" onClick={onClose} aria-label="Close add condition" className="grid min-h-11 min-w-11 place-items-center rounded-lg bg-slate-100 text-xl">×</button></div>
    <form onSubmit={submit} className="mt-5 grid gap-5">{!selected && !customMode ? <><label className="grid gap-1.5 text-sm font-semibold text-slate-700">Search conditions<input ref={searchRef} type="search" className={controlClass} placeholder="Try MS, Sleep Apnea, or IBS" value={search} onChange={(event) => setSearch(event.target.value)}/></label><div><p className="text-sm font-semibold text-slate-700">Filter by category</p><div className="mt-2 flex gap-2 overflow-x-auto pb-2" aria-label="Condition categories"><button type="button" onClick={() => setCategory("")} aria-pressed={!category} className={`min-h-11 shrink-0 rounded-full px-4 text-sm font-semibold ${!category ? "bg-blue-600 text-white" : "bg-slate-100 text-slate-700"}`}>All</button>{categories.map((item) => <button key={item.id} type="button" onClick={() => setCategory(item.id)} aria-pressed={category === item.id} className={`min-h-11 shrink-0 rounded-full px-4 text-sm font-semibold ${category === item.id ? "bg-blue-600 text-white" : "bg-slate-100 text-slate-700"}`}>{item.name}</button>)}</div></div><div aria-live="polite" className="sr-only">{results.length} matching conditions</div><div className="max-h-72 overflow-y-auto rounded-xl border border-slate-200">{results.length ? results.map((item) => <button key={item.id} type="button" onClick={() => setSelected(item)} className="flex min-h-14 w-full items-center justify-between border-b border-slate-100 px-4 py-3 text-left last:border-0 hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-blue-600"><span><span className="block font-semibold text-slate-900">{item.name}{item.short_name ? ` (${item.short_name})` : ""}</span><span className="block text-sm text-slate-500">{item.category?.name}</span></span><span aria-hidden="true">→</span></button>) : <div className="p-6 text-center"><p className="font-semibold text-slate-900">No matching conditions found</p><p className="mt-1 text-sm text-slate-600">Try another search or add a custom condition.</p></div>}</div><button type="button" onClick={() => setCustomMode(true)} className="min-h-11 text-left text-sm font-semibold text-blue-700">Condition not listed? Add a custom condition</button></> : <><div className="rounded-xl border border-blue-200 bg-blue-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-blue-700">{customMode ? "Custom condition" : "Selected condition"}</p>{customMode ? <label className="mt-2 grid gap-1.5 text-sm font-semibold text-slate-700">Condition name<input ref={searchRef} className={controlClass} maxLength={120} value={customName} onChange={(event) => setCustomName(event.target.value)}/></label> : <p className="mt-1 font-semibold text-slate-900">{selected?.name}</p>}<button type="button" onClick={() => { setSelected(null); setCustomMode(false); }} className="mt-2 min-h-11 text-sm font-semibold text-blue-700">Choose a different condition</button></div><ConditionDetailsFields values={details} onChange={setDetails}/></>}{error ? <InlineNotice>{error}</InlineNotice> : null}{selected || customMode ? <div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button type="button" variant="secondary" onClick={onClose}>Cancel</Button><Button type="submit" disabled={saving}>{saving ? "Saving…" : "Add condition"}</Button></div> : null}</form></div></div>;
}
